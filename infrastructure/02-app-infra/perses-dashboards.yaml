apiVersion: perses.dev/v1alpha2
kind: PersesDashboard
metadata:
  name: myreadings-service-health
  namespace: myreadings-dev
spec:
  config:
    duration: "30m"
    display:
      name: "MyReadings — Service Health"

    layouts:
      - kind: "Grid"
        spec:
          display: { title: "Traffic", collapse: { open: true } }
          items:
            - x: 0
              y: 0
              width: 12
              height: 6
              content: { "$ref": "#/spec/panels/reqRate" }
            - x: 12
              y: 0
              width: 12
              height: 6
              content: { "$ref": "#/spec/panels/errRate" }
            - x: 0
              y: 6
              width: 12
              height: 6
              content: { "$ref": "#/spec/panels/latencyAvg" }

      - kind: "Grid"
        spec:
          display: { title: "Container Resources", collapse: { open: true } }
          items:
            - x: 0
              y: 0
              width: 12
              height: 6
              content: { "$ref": "#/spec/panels/cpuPercent" }
            - x: 12
              y: 0
              width: 12
              height: 6
              content: { "$ref": "#/spec/panels/memPercent" }
            - x: 0
              y: 6
              width: 12
              height: 6
              content: { "$ref": "#/spec/panels/restarts" }

    panels:
      reqRate:
        kind: "Panel"
        spec:
          display:
            name: "Request Rate"
            description: "HTTP requests per second, by service"
          plugin:
            kind: "TimeSeriesChart"
            spec:
              legend: { position: "bottom" }
              yAxis:
                format:
                  unit: "requests/sec"
          queries:
            - kind: "TimeSeriesQuery"
              spec:
                plugin:
                  kind: "PrometheusTimeSeriesQuery"
                  spec:
                    datasource: { kind: "PrometheusDatasource", name: "perses-global-ds" }
                    query: >-
                      sum by (exported_job) (
                        rate(http_server_request_duration_seconds_count{
                          exported_job=~"myreadings-.*-service"
                        }[2m])
                      )

      errRate:
        kind: "Panel"
        spec:
          display:
            name: "Error Rate"
            description: "Percentage of HTTP 5xx responses"
          plugin:
            kind: "TimeSeriesChart"
            spec:
              legend: { position: "bottom" }
              yAxis:
                format:
                  unit: "percent"
                  decimalPlaces: 1
          queries:
            - kind: "TimeSeriesQuery"
              spec:
                plugin:
                  kind: "PrometheusTimeSeriesQuery"
                  spec:
                    datasource: { kind: "PrometheusDatasource", name: "perses-global-ds" }
                    query: >-
                      (
                        sum by (exported_job) (
                          rate(http_server_request_duration_seconds_count{
                            exported_job=~"myreadings-.*-service",
                            http_response_status_code=~"5.."
                          }[2m])
                        )
                        /
                        sum by (exported_job) (
                          rate(http_server_request_duration_seconds_count{
                            exported_job=~"myreadings-.*-service"
                          }[2m])
                        )
                      ) * 100

      memPercent:
        kind: "Panel"
        spec:
          display:
            name: "Memory Usage vs Limit (worst pod)"
            description: "Highest per-pod working-set bytes as % of memory limit"
          plugin:
            kind: "TimeSeriesChart"
            spec:
              legend: { position: "bottom" }
              yAxis:
                format:
                  unit: "percent"
                  decimalPlaces: 0
          queries:
            - kind: "TimeSeriesQuery"
              spec:
                plugin:
                  kind: "PrometheusTimeSeriesQuery"
                  spec:
                    datasource: { kind: "PrometheusDatasource", name: "perses-global-ds" }
                    query: >-
                      max by (k8s_container_name) (
                        container_memory_usage_bytes{
                          k8s_namespace_name="myreadings-dev",
                          k8s_container_name=~"(catalog|readinglist|review|user)-service"
                        }
                        / on(k8s_container_name, k8s_pod_name, k8s_namespace_name) group_left()
                        max by (k8s_container_name, k8s_pod_name, k8s_namespace_name) (
                          k8s_container_memory_limit_bytes{
                            k8s_namespace_name="myreadings-dev",
                            k8s_container_name=~"(catalog|readinglist|review|user)-service"
                          }
                        )
                      ) * 100

      restarts:
        kind: "Panel"
        spec:
          display:
            name: "Container Restarts"
            description: "Total restart count (gauge from k8s API)"
          plugin:
            kind: "TimeSeriesChart"
            spec:
              legend: { position: "bottom" }
              yAxis:
                format:
                  unit: "decimal"
                  decimalPlaces: 0
          queries:
            - kind: "TimeSeriesQuery"
              spec:
                plugin:
                  kind: "PrometheusTimeSeriesQuery"
                  spec:
                    datasource: { kind: "PrometheusDatasource", name: "perses-global-ds" }
                    query: >-
                      max by (k8s_container_name, k8s_pod_name) (
                        k8s_container_restarts{
                          k8s_namespace_name="myreadings-dev",
                          k8s_container_name=~"(catalog|readinglist|review|user)-service"
                        }
                      )

      latencyAvg:
        kind: "Panel"
        spec:
          display:
            name: "HTTP Latency (avg)"
            description: "Average request duration by service"
          plugin:
            kind: "TimeSeriesChart"
            spec:
              legend: { position: "bottom" }
              yAxis:
                format:
                  unit: "seconds"
                  decimalPlaces: 3
          queries:
            - kind: "TimeSeriesQuery"
              spec:
                plugin:
                  kind: "PrometheusTimeSeriesQuery"
                  spec:
                    datasource: { kind: "PrometheusDatasource", name: "perses-global-ds" }
                    query: >-
                      sum by (exported_job) (
                        rate(http_server_request_duration_seconds_sum{
                          exported_job=~"myreadings-.*-service"
                        }[2m])
                      )
                      /
                      sum by (exported_job) (
                        rate(http_server_request_duration_seconds_count{
                          exported_job=~"myreadings-.*-service"
                        }[2m])
                      )

      cpuPercent:
        kind: "Panel"
        spec:
          display:
            name: "CPU Usage vs Limit (worst pod)"
            description: "Highest per-pod CPU usage as % of CPU limit"
          plugin:
            kind: "TimeSeriesChart"
            spec:
              legend: { position: "bottom" }
              yAxis:
                format:
                  unit: "percent"
                  decimalPlaces: 0
          queries:
            - kind: "TimeSeriesQuery"
              spec:
                plugin:
                  kind: "PrometheusTimeSeriesQuery"
                  spec:
                    datasource: { kind: "PrometheusDatasource", name: "perses-global-ds" }
                    query: >-
                      max by (k8s_container_name) (
                        container_cpu_usage{
                          k8s_namespace_name="myreadings-dev",
                          k8s_container_name=~"(catalog|readinglist|review|user)-service"
                        }
                        / on(k8s_container_name, k8s_pod_name, k8s_namespace_name) group_left()
                        max by (k8s_container_name, k8s_pod_name, k8s_namespace_name) (
                          k8s_container_cpu_limit{
                            k8s_namespace_name="myreadings-dev",
                            k8s_container_name=~"(catalog|readinglist|review|user)-service"
                          }
                        )
                      ) * 100
